## YAGNI / Scope Control

Build the smallest safe change that satisfies the current task. Do not add speculative abstractions, configuration, extension points, unrelated refactors, or future scenarios. Before editing, ask whether the task would still be complete without the extra work; if yes, omit it.
