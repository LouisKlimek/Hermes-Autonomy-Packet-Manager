# YAGNI addon — skills

SKELETON. This directory holds skills contributed by the YAGNI addon (used by the
`ponytail` mode, which sources its ruleset/skills from the external
`DietrichGebert/ponytail` repo). The concrete skill content is delivered by FR-8.

Layout (per the addon registry schema, `addons/SCHEMA.md`):

```
addons/yagni/skills/
  <skill-name>/
    SKILL.md
```

Kept as a tracked placeholder so the folder structure and `contributes.skills`
declaration validate before FR-8 fills in the content.
