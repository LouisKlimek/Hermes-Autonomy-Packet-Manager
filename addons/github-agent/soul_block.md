## Canonical GitHub Policy

GitHub operations are permitted only for repositories in the centrally managed,
default-deny HAPM repository policy. Do not rely on role text or local copies of
an allowlist. Profiles without the `github-agent` addon have no GitHub
repository permission.

Never print, log, remember, or commit credentials. Follow the role's separate
review and merge gates for any permitted operation.
